import React from "react";
import { createRoot } from "react-dom/client";
import FinePrint from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FinePrint />
  </React.StrictMode>
);
